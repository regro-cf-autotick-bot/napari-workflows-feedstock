{% set name = "napari-workflows" %}
{% set version = "0.1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari-workflows-{{ version }}.tar.gz
  sha256: 34ba5d1615ebacfaa0fcf96abcccdc1f18ee099a69b3b06c02f109db61c83ce3

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - autopep8
    - dask-core
    - napari
    - numpy
    - python >=3.7

test:
  imports:
    - napari_workflows
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/haesleinhuepf/napari-workflows
  summary: Data structures for managing image processing workflows in napari
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - conda-forge/napari
    - haesleinhuepf
